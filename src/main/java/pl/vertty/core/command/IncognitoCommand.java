// 
// Decompiled by Procyon v0.5.36
// 

package pl.vertty.core.command;


import pl.vertty.core.manager.IncognitoManager;
import pl.vertty.api.commands.Command;
/*
@CommandInfo(name = "incognito", player = true)
public class IncognitoCommand extends Command
{
    private final IncognitoManager incognitoManager;
    
    public IncognitoCommand(final IncognitoManager incognitoManager, cn.nukkit.command.Command command) {
        super(command.getName(), "", "", command.getAliases());
        this.incognitoManager = incognitoManager;
    }
    
    @Override
    public void onCommand(final CommandSender commandSender, final String... args) {
        final Player player = (Player)commandSender;
        this.incognitoManager.toggleIncognito(player);
    }
}

 */
